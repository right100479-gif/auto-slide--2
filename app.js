const App=(()=>{const $=id=>document.getElementById(id);function buildFromConti(){const lines=$("contiText").value.split(/\n+/).map(x=>x.trim()).filter(Boolean);const slides=[];for(const line of lines){const m=line.match(/^(.*?)\s*[\(\[]?(\d+)[\)\]]?\s*$/);const title=(m?m[1]:line).trim();const count=m?parseInt(m[2],10):1;for(let i=0;i<count;i++)slides.push(Matcher.prepare({song:title,part:`${i+1}절`,chorus:false,lyrics:""}));}AutoSlideData.slides=slides;renderEditor();}function addSlide(){AutoSlideData.slides.push(Matcher.prepare({song:"",part:"",chorus:false,lyrics:""}));renderEditor();}function renderEditor(){const box=$("slideEditor");box.innerHTML="";AutoSlideData.slides.forEach((s,i)=>{const d=document.createElement("div");d.className="slide-card";d.innerHTML=`<div class="slide-num">${i+1}</div><input placeholder="곡명" value="${esc(s.song)}"><input placeholder="절수 예: 1절" value="${esc(s.part)}"><label><input type="checkbox" ${s.chorus?"checked":""}> 후렴</label><button class="secondary small" type="button">삭제</button><textarea placeholder="이 슬라이드에서 부를 가사를 입력하세요.">${esc(s.lyrics)}</textarea>`;const inputs=d.querySelectorAll("input");const ta=d.querySelector("textarea");const del=d.querySelector("button");inputs[0].oninput=()=>{s.song=inputs[0].value};inputs[1].oninput=()=>{s.part=inputs[1].value};inputs[2].onchange=()=>{s.chorus=inputs[2].checked};ta.oninput=()=>{s.lyrics=ta.value;Matcher.prepare(s)};del.onclick=()=>{AutoSlideData.slides.splice(i,1);renderEditor();};box.appendChild(d);});}function esc(v){return String(v||"").replace(/[&<>"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;"}[c]));}function startLive(){if(!AutoSlideData.slides.length)buildFromConti();AutoSlideData.slides.forEach(Matcher.prepare);$("setupScreen").classList.add("hidden");$("liveScreen").classList.remove("hidden");goToSlide(0);}function backSetup(){SpeechEngine.stop();$("liveScreen").classList.add("hidden");$("setupScreen").classList.remove("hidden");}function goToSlide(i){if(i<0||i>=AutoSlideData.slides.length)return;AutoSlideData.currentIndex=i;SpeechEngine.reset();setProgress(0);renderLive();Projector.update();}function nextSlide(){if(AutoSlideData.currentIndex<AutoSlideData.slides.length-1)goToSlide(AutoSlideData.currentIndex+1);}function prevSlide(){if(AutoSlideData.currentIndex>0)goToSlide(AutoSlideData.currentIndex-1);}function renderLive(){const s=AutoSlideData.slides[AutoSlideData.currentIndex];$("liveLyrics").textContent=s?.lyrics||"가사를 입력하세요";$("liveMeta").textContent=`${AutoSlideData.currentIndex+1} / ${AutoSlideData.slides.length} · ${s?.song||""} ${s?.part||""}`;$("slideCount").textContent=`${AutoSlideData.currentIndex+1} / ${AutoSlideData.slides.length}`;$("songTitle").textContent=s?.song||"-";renderList();}function renderList(){const list=$("slideList");list.innerHTML="";AutoSlideData.slides.forEach((s,i)=>{const d=document.createElement("div");d.className="list-item"+(i===AutoSlideData.currentIndex?" active":"");d.textContent=`${i+1}. ${s.song||"제목 없음"} ${s.part||""}${s.chorus?" [후렴]":""}`;d.onclick=()=>goToSlide(i);list.appendChild(d);});}function setProgress(v){AutoSlideData.progress=v;$("progressText").textContent=v+"%";$("progressBar").style.width=v+"%";}function setMicState(on){$("micState").textContent=on?"ON":"OFF";$("micState").style.color=on?"#10b981":"#ef4444";$("btnMic").textContent=on?"음성 인식 중지":"음성 인식 시작";}function showSpeech(t){$("speechPreview").textContent=t||"...";}function applyProject(p){$("contiText").value=p.conti||"";AutoSlideData.slides=(p.slides||[]).map(Matcher.prepare);renderEditor();}function init(){renderEditor();$("btnBuild").onclick=buildFromConti;$("btnAddSlide").onclick=addSlide;$("btnStart").onclick=startLive;$("btnBack").onclick=backSetup;$("btnPrev").onclick=prevSlide;$("btnNext").onclick=nextSlide;$("btnMic").onclick=()=>SpeechEngine.start();$("btnSave").onclick=Storage.saveLocal;$("btnLoad").onclick=()=>{const p=Storage.loadLocal();if(p)applyProject(p)};$("btnExport").onclick=Storage.download;$("btnProjector").onclick=Projector.open;$("importFile").onchange=async e=>{const f=e.target.files[0];if(!f)return;try{applyProject(await Storage.readFile(f));alert("가져왔습니다.");}catch{alert("파일을 읽을 수 없습니다.");}e.target.value="";};$("jumpNumber").addEventListener("keydown",e=>{if(e.key==="Enter"){const n=parseInt(e.target.value,10);if(n>=1&&n<=AutoSlideData.slides.length)goToSlide(n-1);e.target.value="";}});}return{init,goToSlide,nextSlide,prevSlide,setProgress,setMicState,showSpeech};})();document.addEventListener("DOMContentLoaded",App.init);

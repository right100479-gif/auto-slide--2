const Matcher=(()=>{function clean(s){return String(s||"").replace(/[\s.,/#!$%^&*;:{}=_`~()\-+\[\]"'“”‘’]/g,"");}function normalize(s){return clean(s).toLowerCase();}function chunks(text,size=2){const t=normalize(text);const arr=[];for(let i=0;i<=t.length-size;i++)arr.push(t.slice(i,i+size));return [...new Set(arr)];}function firstPhrase(text,len=14){const t=normalize(text);return t.slice(0,Math.min(len,t.length));}function endingCandidates(text){const t=normalize(text);const lens=[22,18,14,10,7].filter(n=>t.length>=n);return lens.map(n=>t.slice(-n));}function progress(speech,slide){const heard=normalize(speech);const keys=slide.matchKeys||chunks(slide.lyrics,2);if(!keys.length)return 0;let hit=0;for(const k of keys){if(heard.includes(k))hit++;}return Math.round(hit/keys.length*100);}function hasEnding(speech,slide){const heard=normalize(speech).slice(-80);return (slide.endings||endingCandidates(slide.lyrics)).some(e=>e.length>=5&&heard.includes(e));}function hasFirst(speech,slide){const heard=normalize(speech).slice(-80);const f=slide.firstPhrase||firstPhrase(slide.lyrics);return f.length>=5&&heard.includes(f);}function prepare(slide){slide.matchKeys=chunks(slide.lyrics,2);slide.firstPhrase=firstPhrase(slide.lyrics);slide.endings=endingCandidates(slide.lyrics);return slide;}return{clean,normalize,chunks,progress,hasEnding,hasFirst,prepare};})();
